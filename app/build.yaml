targets:
  $default:
    builders:
      json_serializable:
        options:
          # PROTOCOL.md §1: all payload keys are snake_case.
          field_rename: snake
          explicit_to_json: true
