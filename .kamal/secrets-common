# Kamal's secrets for every destination (Kamal reads .kamal/secrets only without
# one), read on the machine that deploys. Only the registry token: the app's secrets
# stay on the server, in the .env both containers read (deploy/deploy.yml).
KAMAL_REGISTRY_PASSWORD=$FAZOURA_REGISTRY_TOKEN
